<div class="account-form-page">
  <div class="container">
    <div class="account-form-card text-center">
      <h1 class="account-form-title">Resend unlock instructions</h1>
      <%= simple_form_for(resource, as: resource_name, url: unlock_path(resource_name), html: { method: :post }) do |f| %>
        <%= f.error_notification %>
        <%= f.full_error :unlock_token %>

        <%= f.input :email,
                    required: true,
                    autofocus: true,
                    placeholder: "Your email",
                    input_html: { class: "placeholder-custom", autocomplete: "email" } %>

        <%= f.submit "Resend unlock instructions", class: "btn button-default w-100 rounded-5 mb-2" %>
      <% end %>
      <div class="d-grid gap-3">
        <%= render "devise/shared/links" %>
      </div>
    </div>
  </div>
</div>
